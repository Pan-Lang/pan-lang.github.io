import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#16AB8D',
      dark: '#119178',
      contrastText: '#FFFFFF',
      borderColor: '#FFFFF5',
      altText: '#26B020',
      light: '#26B020'
    },
  },
});

export default theme;
